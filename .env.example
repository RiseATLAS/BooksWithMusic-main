# ============================================
# BooksWithMusic Environment Configuration
# ============================================
# 
# 🔐 SECURITY: This file contains example values only.
# Create a .env file (already gitignored) with your actual keys.
# NEVER commit the .env file or real API keys to version control!
#
# Copy this file to .env and replace values with your actual keys:
#   cp .env.example .env
#
# 📝 NOTE: For production/GitHub Pages deployment, Firebase variables 
# are already configured as GitHub repository secrets. This .env file
# is only needed for local development.
# ============================================

# Optional: For AI-powered music selection (uses Claude Sonnet 4.5)
VITE_AI_API_KEY=your-anthropic-api-key-here

# Optional: For external music sources
VITE_MUSIC_API_KEY=your-music-api-key-here

# Optional: For cloud storage (S3/R2/B2)
VITE_STORAGE_BUCKET_URL=https://your-bucket.example.com

# Firebase Configuration (Required for online features)
# Get these values from: https://console.firebase.google.com/
# Project Settings > General > Your apps > Firebase SDK snippet > Config
#
# 🚫 Analytics Disabled: measurementId is not used (Analytics disabled for privacy)
# ✅ Only Authentication, Firestore, and Storage are enabled
#
# 🔑 Production: These are stored as GitHub repository secrets for CI/CD
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
